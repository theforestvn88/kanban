<%= render "kanban7/modal" do %>
  <div class="mx-auto w-full">
        <%= render partial: @board_configs.form_card_partial, 
            locals: {
                @board_configs.list_model_symbol => @list, 
                @board_configs.card_model_symbol => @board_configs.card_model.new(@board_configs.list_model_symbol => @list),
                :url => Rails.application.routes.url_helpers.send("#{@board_configs.kanban_name}_kanban_create_#{@board_configs.card_model_name}_path", 
                          "#{@board_configs.list_model_name}_id".to_sym => @list.id)
            } 
        %>
  </div>
<% end %>