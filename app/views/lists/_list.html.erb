<div id="<%= dom_id(list) %>" class="border border-indigo-600 w-96 ml-10">
    <div class="flex justify-between py-2 px-3 border-b border-indigo-600">
        <span><%= list.name %></span>
        <span class="flex justify-end">
            <%= link_to "edit", edit_board_list_path(board_id: list.board_id, id: list.id), data: {turbo_frame: :modal}, class: "text-indigo-600 underline hover:bg-indigo-200" %>
            <%= link_to "delete", board_list_path(board_id: list.board_id, id: list.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure ?"}, class: "text-indigo-600 underline hover:bg-indigo-200 mx-2" %>
        </span>
    </div>

    <%= link_to "+ create new card", new_board_list_card_path(board_id: list.board_id, list_id: list.id), data: { turbo_frame: :modal }, class: "text-gray-600 border border-dashed border-indigo-300 hover:bg-gray-100 px-10 py-5 m-5 flex items-center" %>
</div>